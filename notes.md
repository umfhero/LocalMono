for the promts, they should all regenerate everytime a task/project is completed, marked down etc so its live.

It should specify things i have completed, things coming up, things iv missed etc. It should prioritise english UK langauge , no hyphens etc. 

On project page, increase the size of project containers a bit and include more info like how many files are in each project, progress bars should reflect completed tasks etc. Clicking on a task inside of a project should open that task note. 


On the file itself, code cells are exactly what i wanted, come pre installed with Python, JS, java please.

Allow the side bar of the code preview to be in the midde of the screen eg split screen while open. Also by default on both modes, increase the width of the file page, too much margine on the left and fight. 

On the code preview, add a run button and all other coding tools in a tool bar at the top, also for each language, make sure syntax coloring is enabled and works well.


allow me to click on a image i pasted in to view it in a larger format, maybe even open in a new side bar on the left to allow me to edit it like add text, draw on it. Add more functionality like resizings, cropping images, moving them around the page. 



The sidebar on hover to expand area should expand more to the right, so as i get close is opens, not just over it etc.

Could you also make the sidebar icons a lot bigger.